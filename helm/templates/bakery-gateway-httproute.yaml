{{- if and (.Values.bakery.enabled) .Values.bakery.gateway.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "bakery.fullname" . }}-httproute
  labels:
    {{- include "bakery.labels" . | nindent 4 }}
    app.kubernetes.io/component: bakery-gateway
  {{- with .Values.bakery.gateway.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  hostnames:
  {{- range .Values.bakery.gateway.hostnames }}
  - {{ . | quote }}
  {{- end }}
  parentRefs:
  - name: {{ .Values.bakery.gateway.gatewayName }}
    {{- if .Values.bakery.gateway.gatewayNamespace }}
    namespace: {{ .Values.bakery.gateway.gatewayNamespace }}
    {{- end }}
    {{- if .Values.bakery.gateway.listener.name }}
    sectionName: {{ .Values.bakery.gateway.listener.name }}
    {{- end }}
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /api
    backendRefs:
    - name: {{ include "bakery.fullname" . }}
      port: {{ .Values.bakery.service.port }}
  - matches:
    - path:
        type: PathPrefix
        value: /docs
    backendRefs:
    - name: {{ include "bakery.fullname" . }}
      port: {{ .Values.bakery.service.port }}
  - matches:
    - path:
        type: PathPrefix
        value: /redoc
    backendRefs:
    - name: {{ include "bakery.fullname" . }}
      port: {{ .Values.bakery.service.port }}
  - matches:
    - path:
        type: Exact
        value: /openapi.json
    backendRefs:
    - name: {{ include "bakery.fullname" . }}
      port: {{ .Values.bakery.service.port }}
  - matches:
    - path:
        type: Exact
        value: /metrics
    backendRefs:
    - name: {{ include "bakery.fullname" . }}
      port: {{ .Values.bakery.service.port }}
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: {{ include "bakery.uiFullname" . }}
      port: {{ .Values.bakery.ui.service.port }}
{{- end }}
