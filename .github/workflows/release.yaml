#  ____                       _  ____      _
# |  _ \ ___  _   _ _ __   __| |/ ___|__ _| | _____
# | |_) / _ \| | | | '_ \ / _` | |   / _` | |/ / _ \
# |  __/ (_) | |_| | | | | (_| | |__| (_| |   <  __/
# |_|   \___/ \__,_|_| |_|\__,_|\____\__,_|_|\_\___|
#

name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read
  packages: write

concurrency:
  group: release-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  PACKAGE_OWNER: ${{ vars.GHCR_OWNER || github.repository_owner }}
  REPO_NAME: ${{ vars.GHCR_REPO_NAME || github.event.repository.name }}
  IMAGE_NAME: ${{ format('{0}/{1}', vars.GHCR_OWNER || github.repository_owner, vars.GHCR_REPO_NAME || github.event.repository.name) }}
  CHART_PATH: helm

jobs:
  build-and-push-image:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.meta.outputs.version }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.CR_PAT || github.token }}

      - name: Extract metadata for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          target: bakery-runtime
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=bakery-api
          cache-to: type=gha,mode=max,scope=bakery-api
          platforms: linux/amd64

      - name: Extract metadata for UI Docker
        id: meta-ui
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-ui
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha

      - name: Build and push UI Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./ui/Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta-ui.outputs.tags }}
          labels: ${{ steps.meta-ui.outputs.labels }}
          cache-from: type=gha,scope=bakery-ui
          cache-to: type=gha,mode=max,scope=bakery-ui
          platforms: linux/amd64

      - name: Verify UI Docker image is public
        if: github.event_name != 'pull_request'
        env:
          GH_API_TOKEN: ${{ secrets.CR_PAT || github.token }}
        run: |
          visibility="$(
            curl --fail --silent --show-error \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${GH_API_TOKEN}" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/orgs/${{ env.PACKAGE_OWNER }}/packages/container/${{ env.REPO_NAME }}-ui" \
            | python3 -c 'import json, sys; print(json.load(sys.stdin)["visibility"])'
          )"
          if [ "${visibility}" != "public" ]; then
            echo "Package ${{ env.REPO_NAME }}-ui is ${visibility}, not public." >&2
            echo "Update it in GitHub at https://github.com/orgs/${{ env.PACKAGE_OWNER }}/packages/container/package/${{ env.REPO_NAME }}-ui/settings" >&2
            exit 1
          fi

      - name: Verify Docker image is public
        if: github.event_name != 'pull_request'
        env:
          GH_API_TOKEN: ${{ secrets.CR_PAT || github.token }}
        run: |
          visibility="$(
            curl --fail --silent --show-error \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${GH_API_TOKEN}" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/orgs/${{ env.PACKAGE_OWNER }}/packages/container/${{ env.REPO_NAME }}" \
            | python3 -c 'import json, sys; print(json.load(sys.stdin)["visibility"])'
          )"
          if [ "${visibility}" != "public" ]; then
            echo "Package ${{ env.REPO_NAME }} is ${visibility}, not public." >&2
            echo "Update it in GitHub at https://github.com/orgs/${{ env.PACKAGE_OWNER }}/packages/container/package/${{ env.REPO_NAME }}/settings" >&2
            exit 1
          fi

  build-and-push-chart:
    runs-on: ubuntu-latest
    needs: build-and-push-image
    if: github.event_name != 'pull_request'
    outputs:
      chart_name: ${{ steps.chart_meta.outputs.chart_name }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.13.0

      - name: Resolve chart metadata
        id: chart_meta
        run: |
          chart_name="$(awk -F': ' '/^name:/ {print $2; exit}' "${{ env.CHART_PATH }}/Chart.yaml")"
          if [ -z "$chart_name" ]; then
            echo "Failed to resolve chart name from ${{ env.CHART_PATH }}/Chart.yaml" >&2
            exit 1
          fi
          echo "chart_name=$chart_name" >> "$GITHUB_OUTPUT"

      - name: Log in to Container Registry
        run: |
          echo "${{ secrets.CR_PAT || github.token }}" | helm registry login ${{ env.REGISTRY }} -u ${{ github.actor }} --password-stdin

      - name: Update Helm dependencies
        run: |
          helm dependency update "${{ env.CHART_PATH }}"

      - name: Package Helm chart
        run: |
          mkdir -p .helm-packages
          helm package "${{ env.CHART_PATH }}" -d .helm-packages
          ls -lah .helm-packages/

      - name: Push Helm chart to OCI registry
        run: |
          for chart in .helm-packages/*.tgz; do
            echo "Pushing $chart to oci://${{ env.REGISTRY }}/${{ env.PACKAGE_OWNER }}/charts"
            helm push "$chart" oci://${{ env.REGISTRY }}/${{ env.PACKAGE_OWNER }}/charts
          done

      - name: Verify Helm chart is public
        run: |
          visibility="$(
            curl --fail --silent --show-error \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${{ secrets.CR_PAT || github.token }}" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/orgs/${{ env.PACKAGE_OWNER }}/packages/container/charts%2F${{ steps.chart_meta.outputs.chart_name }}" \
            | python3 -c 'import json, sys; print(json.load(sys.stdin)["visibility"])'
          )"
          if [ "${visibility}" != "public" ]; then
            echo "Package charts/${{ steps.chart_meta.outputs.chart_name }} is ${visibility}, not public." >&2
            echo "Update it in GitHub at https://github.com/orgs/${{ env.PACKAGE_OWNER }}/packages/container/package/charts%2F${{ steps.chart_meta.outputs.chart_name }}/settings" >&2
            exit 1
          fi

  create-release:
    runs-on: ubuntu-latest
    needs: [build-and-push-image, build-and-push-chart]
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
      packages: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Resolve release version
        id: release_meta
        run: |
          echo "chart_version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          body: |
            ## Installation

            ### Docker Images
            ```bash
            docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.release_meta.outputs.chart_version }}
            docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-ui:${{ steps.release_meta.outputs.chart_version }}
            ```

            ### Helm Chart
            ```bash
            helm install bakery oci://${{ env.REGISTRY }}/${{ env.PACKAGE_OWNER }}/charts/${{ needs.build-and-push-chart.outputs.chart_name }} --version ${{ steps.release_meta.outputs.chart_version }}
            ```
